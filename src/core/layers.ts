/** Layer for objects that receive bloom post-processing. */
export const BLOOM_LAYER = 1;

/** Layer for the lightbox overlay + zoomed image, rendered after clean + bloom. */
export const LIGHTBOX_LAYER = 2;
